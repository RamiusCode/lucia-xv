---
// Despedida.astro
---

<section class="py-12 px-4 relative overflow-hidden">
  <!-- Fondo sutil -->
  <div class="absolute inset-0 bg-gradient-to-b from-[#fffaf5]/30 via-transparent to-[#fff5f5]/30 z-0"></div>

  <div class="max-w-5xl mx-auto relative z-10">


    <div class="max-w-3xl mx-auto text-center">

      <!-- 🌸 FLORES SUPERIORES -->
      <div class="flex justify-center mb-8" id="flores-arriba">
        <img
          src="/imagenes/FlorArribaSeparador.webp"
          alt="Flores decorativas"
          class="w-48 md:w-56 lg:w-64 h-auto object-contain opacity-80"
        />
      </div>

      <!-- CARD CONTENEDOR DEL MENSAJE -->
      <div class="relative backdrop-blur-sm bg-white/70 rounded-3xl border border-[#a27735]/20 shadow-lg p-8 md:p-10 lg:p-12" id="card-despedida">
        
        <!-- Brillo superior -->
        <div class="absolute top-0 left-0 right-0 h-1/3 bg-gradient-to-b from-white/30 to-transparent rounded-t-3xl pointer-events-none"></div>

        <!-- Esquinas decorativas -->
        <div class="absolute top-2 left-2 w-12 h-12 border-t-2 border-l-2 border-[#a27735]/30"></div>
        <div class="absolute top-2 right-2 w-12 h-12 border-t-2 border-r-2 border-[#a27735]/30"></div>
        <div class="absolute bottom-2 left-2 w-12 h-12 border-b-2 border-l-2 border-[#a27735]/30"></div>
        <div class="absolute bottom-2 right-2 w-12 h-12 border-b-2 border-r-2 border-[#a27735]/30"></div>

        <div class="relative z-10">

          <!-- MENSAJE PRINCIPAL -->
          <div id="mensaje-despedida">
            <p class="font-charm text-lg md:text-xl lg:text-2xl text-[#95303a] leading-relaxed mb-6">
              Con la bendición de Dios y el amor de toda mi familia,
              <span class="text-[#a27735] font-bold">me siento muy feliz</span>
              de llegar a este momento tan especial de mi vida.
            </p>

            <p class="font-charm text-lg md:text-xl lg:text-2xl text-[#95303a] leading-relaxed mb-6">
              Ahora empieza un camino lleno de
              <span class="text-[#a27735] font-bold">sueños e ilusiones</span>
              que, con mucha fe, humildad y esperanza, deseo cumplir.
            </p>

            <p class="font-charm text-lg md:text-xl lg:text-2xl text-[#95303a] leading-relaxed mb-6">
              Quisiera, con inmensa alegría,
              <span class="text-[#a27735] font-bold">compartir este comienzo</span>
              de esta bella etapa junto a todos ustedes.
            </p>

            <!-- Línea decorativa -->
            <div class="flex items-center justify-center gap-3 my-4">
              <div class="w-16 h-[1px] bg-gradient-to-r from-transparent to-[#a27735]"></div>
              <svg class="w-6 h-6 text-[#a27735]" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
              </svg>
              <div class="w-16 h-[1px] bg-gradient-to-l from-transparent to-[#a27735]"></div>
            </div>

          

          </div>

          <!-- SECCIÓN CONFIRMAR ASISTENCIA -->
          <div id="seccion-confirmar" class="mt-2">
            
            <!-- Texto de invitación a confirmar -->
            <p class="font-charm text-lg md:text-xl text-[#95303a] leading-relaxed mb-2">
              Tu presencia es muy importante para mí.
              <br />
              Por favor, <span class="text-[#a27735] font-bold">confirma tu asistencia</span> para compartir juntos este día tan especial.
            </p>

          <!-- BOTÓN CONFIRMAR ASISTENCIA -->
            <div class="flex justify-center" id="boton-confirmar">
            <a
                href="https://wa.me/59178762918?text=Hola,%20confirmo%20mi%20asistencia%20a%20tus%20XV%20años"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Confirmar asistencia por WhatsApp"
                title="Confirmar asistencia por WhatsApp"
            >
                <img
                src="/imagenes/ConfirmarAsistencia.png"
                alt="Confirmar Asistencia"
                class="w-56 md:w-64 lg:w-72 h-auto object-contain cursor-pointer transition-transform duration-300 hover:scale-105"
                />
            </a>
            </div>

              <!-- MENSAJE FINAL -->
            <p class="font-charm pt-10 text-2xl md:text-3xl lg:text-4xl text-[#a27735] font-bold italic mb-8">
              ¡Los espero con mucho cariño!
            </p>


          </div>

        </div>

        <!-- Resplandor del card -->
        <div class="absolute -inset-1 bg-gradient-to-br from-[#a27735]/5 via-transparent to-[#c9a55e]/5 rounded-3xl blur-xl -z-10"></div>

      </div>


    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  if (typeof window !== 'undefined') {
    document.addEventListener("DOMContentLoaded", () => {
      console.log("💌 Iniciando sección de despedida...");

      // 🎯 ESTADO INICIAL
      gsap.set("#flores-arriba", { opacity: 0, y: -30, scale: 0.8 });
      gsap.set("#card-despedida", { opacity: 0, y: 40, scale: 0.95 });
      gsap.set("#flor-despedida-izq", { opacity: 0, x: -40 });
      gsap.set("#flor-despedida-der", { opacity: 0, x: 40 });
      gsap.set("#mensaje-despedida p", { opacity: 0, y: 20 });
      gsap.set("#seccion-confirmar", { opacity: 0, y: 30 });
      gsap.set("#flores-abajo", { opacity: 0, y: 30, scale: 0.8 });

      // ✨ ANIMACIÓN
      const tl = gsap.timeline({
        scrollTrigger: {
          trigger: "#flores-arriba",
          start: "top 85%",
          end: "top 20%",
          toggleActions: "play none none reverse",
        },
      });

      // 1. Flores superiores
      tl.to("#flores-arriba", {
        opacity: 0.8,
        y: 0,
        scale: 1,
        duration: 1,
        ease: "back.out(1.5)",
      })

      // 2. Card aparece
      .to("#card-despedida", {
        opacity: 1,
        y: 0,
        scale: 1,
        duration: 1.2,
        ease: "power3.out",
      }, "-=0.5")

      // 3. Flores laterales
      .to("#flor-despedida-izq, #flor-despedida-der", {
        opacity: 0.4,
        x: 0,
        duration: 1,
        ease: "power2.out",
      }, "-=0.8")

      // 4. Párrafos aparecen en cascada
      .to("#mensaje-despedida p", {
        opacity: 1,
        y: 0,
        duration: 0.8,
        stagger: 0.3,
        ease: "power2.out",
      }, "-=0.5")

      // 5. Sección confirmar asistencia
      .to("#seccion-confirmar", {
        opacity: 1,
        y: 0,
        duration: 1,
        ease: "power2.out",
      }, "-=0.3")

      // 6. Flores inferiores
      .to("#flores-abajo", {
        opacity: 0.7,
        y: 0,
        scale: 1,
        duration: 1,
        ease: "back.out(1.5)",
      }, "-=0.5");

      // 🌸 ANIMACIONES CONTINUAS
      gsap.to("#flor-despedida-izq, #flor-despedida-der", {
        y: -10,
        duration: 4,
        ease: "sine.inOut",
        yoyo: true,
        repeat: -1,
        delay: 2,
      });

      // Corazón pulsando
      gsap.to("#mensaje-despedida svg", {
        scale: 1.2,
        duration: 1.5,
        ease: "sine.inOut",
        yoyo: true,
        repeat: -1,
        delay: 2,
      });

      // Botón pulsando
      gsap.to("#boton-confirmar img", {
        scale: 1.05,
        duration: 2,
        ease: "sine.inOut",
        yoyo: true,
        repeat: -1,
        delay: 3,
      });

      console.log("💌✨ ¡Sección de despedida activada!");
    });
  }
</script>

<style>
  /* Efecto hover en el card */
  #card-despedida:hover {
    box-shadow: 0 20px 40px rgba(162, 119, 53, 0.15);
    transition: box-shadow 0.5s ease;
  }

  /* Resaltar palabras importantes */
  #mensaje-despedida span {
    transition: all 0.3s ease;
  }

  #mensaje-despedida span:hover {
    color: #c9a55e;
    text-shadow: 0 0 10px rgba(162, 119, 53, 0.3);
  }
</style>